# Nested upstream clone (updated via git pull inside it)
upstream/

# Build artifacts
dist/
*.deb
*.tar.gz

# Caches
.cache/
__pycache__/
*.pyc
node_modules/

# Secrets / local tokens
.agents/

# OS noise
.DS_Store

# Internal docs — kept local/private only, never pushed (2026-09-16)
docs/
AGENTS.md
HANDOFF.md
PROJECT_HANDOVER_DOCS.md
